- if @posts.any?
  - @posts.each do |post|
    main.container
      .row.g-5.justify-content-center
        .col-md-10
          article.blog-post
            h2.blog-post-title
              = link_to post.title, post_path(post), class: 'nav-link px-1 link-dark text-underline-hover'
            p.blog-post-meta
              | Дата публикации: 
              = post.created_at.strftime("%d.%m.%Y %H:%M")
            a
            br
            |  Автор: 
            =post.user.name
            p
              = post.content[0..1000]
            .row
              .col-sm-8.col-sm-offset-1.col-md-5.col-md-offset-3
                - if post.content.length > 1000
                  i
                    = link_to 'Читать далее...', post, class: 'nav-link link-dark text-underline-hover'
  footer.blog-footer
    p

    a.nav-link.link-dark.justify-content-md-end.text-underline-hover[href="#"]
      | Наверх
