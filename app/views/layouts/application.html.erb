<!DOCTYPE html>
<html>
  <head>
    <title>TestBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-bold fs-4">Kit-Blog</h5>

      <br>

      <% if user_signed_in? %>

        <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
          <span
            class="me-3 py-2 text-dark text-decoration-none">Текущий пользователь: <%= link_to current_user.email, edit_user_registration_path %>
          </span>

          <span
            class="me-3 py-2 text-dark text-decoration-none"><%= link_to "Главная страница", posts_path %>
          </span>

          <span
            class="me-3 py-2 text-dark text-decoration-none"><%= link_to "Добавить пост", new_post_path %>
          </span>

          <span
            class="me-3 py-2 text-dark text-decoration-none"><%= link_to "Выйти", destroy_user_session_path, :method => :delete %>
          </span>
        </nav>

        <% else %>
        <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
            <span
              class="me-3 py-2 text-dark text-decoration-none"><%= link_to 'Логин', new_user_session_path %>
            </span>

            <span
              class="me-3 py-2 text-dark text-decoration-none"><%= link_to 'Регистрация', new_user_registration_path %>
            </span>
        </nav>
        <% end %>

        <% if can? :read, @users%>
          <span
            class="me-3 py-2 text-dark text-decoration-none"><%= link_to "Пользователи", users_path %>
          </span>
        <% end %>
    </div>

    <div class="container">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
    </div>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
